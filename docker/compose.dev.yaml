---
volumes:
  pgdata:

name: whatitdoo-dev
services:
  # server:
  #   container_name: wid-server
  #   build:
  #     context: ..
  #   ports:
  #     - "8081:8080"
  #   environment:
  #     - WID_DATABASE_HOST=database
  #     - WID_DATABASE_USER=postgres
  #     - WID_DATABASE_PASSWORD=postgres
  #     - WID_DATABASE_NAME=whatitdoo
  #   volumes:
  #     - ./wid.example.yaml:/etc/whatitdoo/wid.yaml:ro
  #   depends_on:
  #     - database

  redis:
    container_name: wid-redis
    image: redis:8
    ports:
      - "6379:6379"

  database:
    container_name: wid-database
    image: postgres:15
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data